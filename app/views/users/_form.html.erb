<section id="form">
  <%= form_for(@user) do |f| %>
    <% if @user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  <div id="user_form">
    <%= f.text_field :name, placeholder: "What's ya name?" %>
    <%= f.number_field :guests, placeholder: "# of guests?" %>
    <%= t.text_field :dish, placeholder: "What are ya bringin?" %>

    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
  <% end %>
</section>
